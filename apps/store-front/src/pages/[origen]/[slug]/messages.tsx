/**
 * Página de mensajes con contexto (grupo/sucursal)
 * Reutiliza la misma lógica pero con contexto
 */

import MessagesPage from '../../messages';

export default function ContextualMessagesPage() {
  return <MessagesPage />;
}

